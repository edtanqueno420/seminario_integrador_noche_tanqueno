let piezaActual: string | null = null;

let proximaPieza: string | null = null;

piezaActual = "PISTON-2026";
console.log(piezaActual);
let piezaRespaldo: string | null = null;
piezaRespaldo = null;
console.log(piezaRespaldo);

let codigoLote: string | undefined;
console.log(codigoLote);

codigoLote = "LOTE-A-4400";
console.log(codigoLote);
